# Specify staging app servers here. Test provisioning and deployment changes in
# staging before making changes "live" in production. Note that setting the
# app_fqdn variable here will allow nginx to respond to requests made to its
# FQDN instead of the production server's FQDN.

deployment-workflow-staging.bocoup.com app_fqdn=deployment-workflow-staging.bocoup.com
