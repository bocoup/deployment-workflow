# Specify production app servers here. Once provisioning and deployment have
# been tested in vagrant or in staging, make changes "live" here. This should
# match the ansible global app_fqdn setting.

deployment-workflow.bocoup.com
